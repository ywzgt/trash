name: win
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run Script
        run: |
          git clone --depth=1 -b 113.0.5672.116 https://chromium.googlesource.com/chromium/src
          dir
        shell: cmd
