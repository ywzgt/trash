name: win
on:
  workflow_dispatch:

jobs:
  run:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run Script
        run: |
          C:\msys64\usr\bin\bash.exe
          df -Th
          echo $PATH
          pacman --version
          winver
          echo ' '
          env
          pacman -Sy
