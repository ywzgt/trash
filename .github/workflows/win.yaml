name: win
on:
  workflow_dispatch:

jobs:
  run:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run Script
        run: |
          msys2bash.cmd
          df -Th
          du -sh /*
          echo $PATH
          winver
