name: win
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run Script
        run: |
          set PATH=C:\msys64\usr\bin;%PATH%
          pip install httplib2 six
          python3 py
        shell: cmd
