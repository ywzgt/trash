name: win
on:
  workflow_dispatch:

jobs:
  run:
    runs-on: windows-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run Script
        run: |
          export PATH=/c/msys64/usr/bin:$PATH
          df -Th
          echo $PATH
          echo
          df -Tha
          echo
          env
          python3 --version
          which python
          which python3
          which git
          which split
          which df
          which timeout
          echo
          timeout --help
          echo
          which pacman
          pacman --version
          pacman -Sy
          false || true
          /bin/timeout 5 sleep 6 || true
        shell: C:\msys64\usr\bin\bash.exe -e {0}
